<!DOCTYPE html>
<html>
<head>
    <title>Tabela Fixa</title>
</head>
<body>
    <div id="app"></div>

    <script src="libs/react.js"></script>
    <script src="libs/react-dom.js"></script>

    <script>
        ReactDOM.render(
            React.createElement(
                'table',
                { border: '1', style: { width: '100%', textAlign: 'center' } },
                [
                    React.createElement(
                        'thead',
                        null,
                        React.createElement(
                            'tr',
                            null,
                            [
                                React.createElement('th', null, 'Coluna 1'),
                                React.createElement('th', null, 'Coluna 2'),
                            ]
                        )
                    ),
                    React.createElement(
                        'tbody',
                        null,
                        React.createElement(
                            'tr',
                            null,
                            [
                                React.createElement('td', null, 'Linha 1, C1'),
                                React.createElement('td', null, 'Linha 1, C2'),
                            ]
                        )
                    )
                ]
            ),
            document.getElementById('app')
        );
    </script>
</body>
</html>
